
import React from "react";
import { View } from "react-native";

const RegistrationFooter = ({onPress, text}) => {
  return (
    <View style={{
      height: 100,
      width: '100%',
      position: 'absolute',
      bottom: 0,
      backgroundColor: '#fff'
    }}>
    </View>
  );
}

export default RegistrationFooter